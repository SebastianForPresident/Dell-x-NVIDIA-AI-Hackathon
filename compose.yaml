services:
  mongo:
    image: mongodb/mongodb-community-server:latest
    container_name: crop-forensics-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
